---
import { normalizeImageUrl } from "../lib/image-url";
import type { RuntimeScore } from "../lib/scores-store";

type Props = { scores: RuntimeScore[] };
const { scores } = Astro.props as Props;
---

<section id="scores" class="section container" data-carousel-root>
    <div class="section-head">
        <div>
            <h2 class="h2">出分展示</h2>
            <p class="muted">真实案例滚动展示（后续换成你的截图即可）</p>
        </div>
        <div class="btn-row">
            <button class="btn" type="button" data-prev>←</button>
            <button class="btn" type="button" data-next>→</button>
        </div>
    </div>

    <div class="carousel">
        <div class="carousel-track" data-track>
            {
                scores.map((s) => (
                    <div class="card score-card">
                        <div class="muted text-sm">
                            {s.exam}
                            {s.date ? ` · ${s.date}` : ""}
                        </div>
                        <div class="h3 mt-2">{s.scoreText}</div>
                        {s.highlight ? (
                            <div class="muted mt-1">{s.highlight}</div>
                        ) : null}
                        <img
                            class="score-img"
                            src={normalizeImageUrl(s.image)}
                            alt={String(s.scoreText ?? s.exam ?? "")}
                        />
                    </div>
                ))
            }
        </div>
    </div>

    <script is:inline>
        (() => {
            const root = document.currentScript?.closest(
                "[data-carousel-root]",
            );
            if (!root) return;

            const track = root.querySelector("[data-track]");
            if (!(track instanceof HTMLElement)) return;

            const prev = root.querySelector("[data-prev]");
            const next = root.querySelector("[data-next]");

            const step = () =>
                Math.max(320, Math.floor(track.clientWidth * 0.9));

            prev?.addEventListener("click", () => {
                track.scrollBy({ left: -step(), behavior: "smooth" });
            });

            next?.addEventListener("click", () => {
                track.scrollBy({ left: step(), behavior: "smooth" });
            });
        })();
    </script>
</section>
